Mon 04 Feb 2019 11:19:23 PM PST 
Pygame crash course.

import sys 

import pygame

from pygame.sprite import Sprite

from pygame.sprite import Group

pygame.init()

screen = pygame.display.set_mode()

pygame.display.set_caption()

background_color = (145, 52, 75)

screen.fill(background_color)

pygame.event.get()

for event in pygame.event.get():
    if event.type == pygame.QUIT:
        sys.exit()
    
    if event.type == pygame.KEYDOWN:
        (insert code here)
    
    if event.type == pygame.KEYUP:
        (insert code here)
    
    if event.key == pygame.K_SPACE:
        (insert code here)
        
    if event.key == pygame.K_UP:
        (insert code here)
        
    if event.key == pygame.K_DOWN:
        (insert code here)
        
    if event.key == pygame.K_LEFT:
        (insert code here)
        
    if event.key == pygame.K_RIGHT:
        (insert code here)
        
    if event.key == pygame.K_q:
     	sys.exit()
        
variable_name.blitme()
variable_name.draw()
variable_name.add()

pygame.display.flip()

variable_name.sprites()

pygame.draw.rect(self.screen, self.rect)

self.screen = screen

self.rect = pygame.Rect(0, 0, item_width, item_height)

self.image = pygame.image.load('file/directory')
self.rect = self.image.get_rect()

screen_rect = screen.get_rect()

self.rect.centerx	# image rectangle on the center of x-axis
self.rect.centery	# image rectangle on the center of y-axis
self.rect.top		# image rectangle on the top of screen
self.rect.bottom	# image rectangle on the bottom of screen
self.rect.left		# image rectangle on the left of screen
self.rect.right		# image rectangle on the right of screen
self.rect.center	# image rectangle on the center of screen
self.rect.width		# image rectangle's width
self.rect.height	# image rectangle's height
self.rect.x		# image rectangle's x-axis
self.rect.y		# image rectangle's y-axis

sprite.groupcolllide()	# looks for collisions between members of two groups
    
        
